<div class="results">
    {{#each movies}}
    <div >
        <img width="200px" src="{{this.image}}" alt="{{this.title}}" />
        <h3>{{this.title}}</h3>
        <a href="/movies/{{this._id}}">See more</a>  
    </div>
    {{/each}}
</div>
